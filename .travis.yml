language : php
php :
  - "7.1"
  - "7.2"

before_script :
  - composer self-update
  - composer install

script :
  - ./vendor/bin/phpunit

cache :
  directories :
    - vendor

notifications :
  on_success : never
  on_failure : never
